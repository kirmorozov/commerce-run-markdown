{
  "name": "commercerun/markdown",
  "description": "Basic markdown that supports links",
  "license": "MIT",
  "require": {
    "ext-dom": "*"
  },
  "require-dev": {
    "phpunit/phpunit": "10"
  },
  "autoload": {
    "psr-4": {
      "CommerceRun\\": "src"
    }
  },
  "scripts": {
    "test": "XDEBUG_MODE=coverage ./vendor/bin/phpunit --exclude-group long_running --coverage-text"
  }
}
